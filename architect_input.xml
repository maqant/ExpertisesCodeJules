<project_context>
Application: ExpertisesCodeJules. Stack: React/Vite. Domaine: courtage assurance, Bureau Péchard, sinistres, rapports d’expertise et annexes. Finalité: ingérer données déstructurées pour générer/vérifier/imprimer/exploiter un rapport fiable, maintenable, évolutif. Contraintes: fiabilité totale, zéro erreur silencieuse, évolutivité avant/pendant/post sinistre, séparation IA/config/état/métier/UI/rapport/export/stockage, UX claire.
</project_context>
<initial_user_request>C'est pas terrible. Ca devrait être
"Bonjour Mnsieur x, 
Bonjour Monsieur x,. Ecrit avec juste la première lettre du nom de famille en majuscule, pas tout en majuscule</initial_user_request>
<business_intent>Améliorer la formule d'appel des e-mails (générée par `buildSalutation`) pour que les noms de famille ne soient plus en ALL CAPS (ex: "MOSCA"), mais en Title Case (ex: "Mosca"). L'utilisateur prend en exemple "Bonjour Monsieur x", ce qui implique aussi que si la civilité est là, elle doit être utilisée avec le nom de famille en Title Case, ou si elle est absente, afficher le prénom + nom en Title Case correctement plutôt qu'un bloc tout en majuscule.</business_intent>
<technical_discovery>
- `buildSalutation` est dans `src/services/utils/contactUtils.js`.
- Elle itère sur les `contacts` et construit une string.
- Actuellement, elle utilise `c.civility` + `lastName` (si civility existe), sinon elle fallback sur `c.displayName` ou `c.nom`.
- Les noms (`lastName` ou `displayName`) proviennent directement de l'extraction, souvent en majuscules (ex: "ABD EL ALIM ABD EL KADER").
- Il n'y a pas de fonction pour convertir un nom "ALL CAPS" en "Title Case" dans `contactUtils.js` actuellement pour `buildSalutation`.
</technical_discovery>
<current_architecture_and_code>
<file path="src/services/utils/contactUtils.js">
<role>Utilitaires de gestion des contacts</role>
<relevance>Contient `buildSalutation` et `parseFullName`.</relevance>
<full_source_code>
// Code de buildSalutation:
export const buildSalutation = (contacts = []) => {
  const list = Array.isArray(contacts) ? contacts : [];
  if (list.length === 0) return 'Bonjour,';

  const parts = list.map((c) => {
    const rawLastName = c?.nom || c?.displayName || '';
    const lastName = typeof rawLastName === 'string' && rawLastName.includes(' ')
      ? (parseFullName(rawLastName)?.lastName || rawLastName)
      : rawLastName;

    if (c?.civility && lastName) {
      return `${c.civility} ${lastName}`.trim();
    }

    if (lastName && lastName !== c?.email) {
      return (c?.displayName || c?.nom || '').trim();
    }

    return (c?.displayName || c?.nom || '').trim();
  });

  const validParts = parts.filter(Boolean);
  if (validParts.length === 0) return 'Bonjour,';

  return `Bonjour ${validParts.join(', ')},`;
};
</full_source_code>
</file>
</current_architecture_and_code>
<code_collection_issue>
<file path="src/services/utils/contactUtils.js"><reason>Fichier partiellement inclus (seulement `buildSalutation`).</reason><known_partial_information>On doit modifier `buildSalutation` et possiblement introduire une fonction `toTitleCase(str)`.</known_partial_information><audit_consequence>ÉLEVÉE: Claude ne dispose pas du fichier complet. Il ne doit pas fournir de patch final directement applicable à ce fichier. Il peut fournir décision architecturale, structure cible, composant isolé si pertinent et instructions d’intégration prudentes. Gemini devra intégrer localement après inspection complète.</audit_consequence></file>
</code_collection_issue>
<related_files_not_fully_included></related_files_not_fully_included>
<current_problem_analysis>
L'affichage brutal du `displayName` ou du `lastName` en majuscule produit une salutation agressive et inélégante ("Bonjour Iman ABD EL ALIM ABD EL KADER, Therry MOSCA,"). L'absence de civilité (souvent non détectée si juste "Propriétaire") force le fallback sur le nom complet. 
La solution doit:
1. Implémenter un formatage `toTitleCase` robuste (gérant les traits d'union, espaces, particules "de", "d'").
2. L'appliquer systématiquement aux noms dans `buildSalutation`.
</current_problem_analysis>
<proposed_technical_paths>
A: Créer une fonction utilitaire `toTitleCase` dans `contactUtils.js` et modifier `buildSalutation` pour l'utiliser sur `lastName` et `displayName`. Si `c.civility` est vide, renvoyer au moins `Prénom Nom` (en Title Case) au lieu du nom brut.
B: Modifier la normalisation en amont (`toContact`) pour que `nom` et `prenom` soient toujours en Title Case dans tout le système (risque d'impact sur les autres formulaires).
</proposed_technical_paths>
<architect_questions_for_claude>
1. Préfères-tu l'option A (formatage local à l'affichage) ou B (normalisation globale) ?
2. Comment gérer au mieux la fonction `toTitleCase` pour ne pas casser les noms complexes (ex: "d'Oultremont", "De La Tour") ?
3. Peux-tu fournir le patch conceptuel pour `buildSalutation` ?
</architect_questions_for_claude>
<directive>
Tu es Claude Fable, Architecte Principal. Lis contexte, code, limites de collecte, demande métier.
1 Challenge: vrai logiciel modulaire ou rustine? 2 Audit: typage, erreurs, couplage, état, appels IA, config, sécurité, maintenabilité, lisibilité, testabilité, UX, métier, rétrocompatibilité, risques fichiers incomplets. 3 Décide clairement; impose mieux si besoin; ne donne pas de patch final pour fichier déclaré incomplet. 4 Fournis IMPLEMENTATION_INSTRUCTIONS_FOR_GEMINI avec ordre exact, fichiers à créer/modifier/ne pas modifier sans inspection complète, fonctions, imports, état React, UI, validations, erreurs, migrations localStorage, comportements à préserver, pièges, non-régression. 5 Code final uniquement pour fichiers complets; sinon composant isolé/structure cible/patch conceptuel. 6 Checklist: tests manuels/techniques, cas limites, UX, métier, non-régression, vérification fichiers incomplets. Critère: fiabilité rapport améliorée, couplage réduit, impacts visibles, erreurs silencieuses évitées, extension future préparée.
</directive>
